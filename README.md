# Github-notification-discord-bot
This is an application that scrapes specified Github repository's commit logs and detects changes, if it finds any then it will send out a notification that includes a the name of the commit to a given discord server/channel! Works with multiple repositories at a time!

## Dependencies
* `bs4`
* `selenium`
* `json`
* `requests`
